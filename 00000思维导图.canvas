{
	"nodes":[
		{"id":"92f94354e2870099","type":"file","file":"SingleMonoBase.md","x":-705,"y":140,"width":400,"height":50,"color":"1"},
		{"id":"64d80c76a07da372","type":"text","text":"它的设计分为三个核心层面：\n\n**首先是接入与约束层**：这个基类继承自 `MonoBehaviour`，让管理类能够接入 Unity 的生命周期。为了做到高复用并保证类型绝对安全，我引入了 **CRTP（奇异递归模板模式）**，也就是 `where T : SingleMonoBase<T>` 这句约束。它在代码编译期就锁死了泛型类型，杜绝了别的脚本错误继承导致的串号崩溃问题。\n\n**其次是内存与访问层**：我抛弃了传统低效的 `Find` 或 `GetComponent` 查找方式。而是声明了一个 `public static T INSTANCE` 静态变量。利用 `static` 的特性，把这个管家实例脱离了具体的游戏物体，放进了全局唯一的静态内存区中。这相当于给全项目拉了一根常驻的专线，访问速度是极快的。\n\n**最后是生命周期防冲突层**：在 Unity 的 `Awake` 阶段，我加入了排他性的冲突检测机制。如果静态实例为空，它就会将自身 `this` 绑定上去；如果发现实例已经存在，则会立刻抛出严重报错。而在 `OnDestroy` 时会主动清空内存，防止内存泄漏。\n\n**总结来说**，这个基类通过**泛型约束保证安全、静态变量保证性能、生命周期管控保证唯一性**。我的 `GameManager`、`UIManager` 等所有核心管家都继承了它，极大地提升了整个项目的解耦能力和底层健壮性。”","x":-916,"y":243,"width":611,"height":50},
		{"id":"3d459b71038c4118","type":"file","file":"GameManager.md","x":-120,"y":140,"width":400,"height":50,"color":"1"},
		{"id":"8ec2d674fc7557fe","type":"file","file":"UIManager.md","x":-120,"y":-315,"width":400,"height":50,"color":"1"},
		{"id":"f6ca4c4533e055be","type":"file","file":"MonoManager.md","x":-80,"y":560,"width":400,"height":50,"color":"1"},
		{"id":"f3ff631ac6d758f4","type":"file","file":"PlayerController.md","x":-80,"y":1040,"width":400,"height":50,"color":"1"},
		{"id":"1a075a9c705c5cec","type":"text","text":"👑 模块一：最高优先级与单例声明\n[DefaultExecutionOrder(100)] \npublic class PlayerController : SingleMonoBase<PlayerController>\n\n**单词解释**：`Default` (默认)，`Execution` (执行)，`Order` (顺序)。\n- **大白话逻辑**：在 Unity 中，如果有 100 个脚本都在运行 `Update`，默认情况下谁先执行是随机的。加上 `(100)` 意味着**强行延后执行**。\n- 面试官：“为什么要加 DefaultExecutionOrder(100)？”\n- - - _你回答：“因为这个脚本负责读取玩家的输入（键盘鼠标）。强行把它的执行顺序设为 100（晚于默认的 0），是为了确保在每一帧中，它读取到的按键数据是绝对最新、最稳定的，防止其他脚本在同一帧内抢先执行导致输入丢失或判定漂移。”_\n\n\n模块二：庞大的变量声明（控制器的按键池）\n这部分代码非常长，但全部都是 `public` 暴露给面板的变量。我们分类看核心：\n\n相机与 UI (`CinemachineFreeLook` / `GameObject`)\n**单词**：`freeLookCamera` (第三人称自由相机)，`aimingCamera` (瞄准时的越肩相机)，`deathPanel` (死亡黑屏UI)。\n**逻辑**：把虚拟相机和 UI 画布的内存地址存下来，方便后面用代码去切换它们。\n\n高级音频池 (`AudioClip[]`)\n**单词**：`AudioClip` (音频片段)，`[]` (数组/列表)，`runSounds` (跑步声)，`aimInSound` (举枪摩擦声 - Foley)。\n**逻辑**：不用单个声音，而是用数组装好几个不同的脚步声，为后面的“防机械音”做准备。\n\n攀爬系统判定参数 (`float`, `LayerMask`)\n**单词**：`minClimbDistance` (最小触发距离)，`maxClimbAngle` (最大触发角度)，`climbableLayer` (可攀爬图层)。\n**逻辑**：定义了物理规则。多远能爬？多斜的墙不能爬？都在这里定死数值。\n\n下蹲平滑参数 (`Vector3`, `SmoothDamp`)\n、**单词**：`standHeight` (站立高度)，`crouchHeight` (下蹲高度)，`crouchSmoothTime` (下蹲平滑时间)。\n\n玩家输入缓存 (`bool`, `Vector2`)\n**单词**：`moveInput` (移动按键方向)，`isSprint` (是否按了冲刺)，`isAiming` (是否按了右键)。\n**逻辑**：把键盘上的物理按键，转化成了电脑内存里的“真”和“假”。\n\n\n模块三：通电初始化 (`Awake` & `Start`)\nprotected override void Awake() { input = new Myinputsystem(); }\n\n**逻辑**：实例化你配置好的 Unity 新输入系统（New Input System）。\n\nCursor.lockState = CursorLockMode.Locked;\n- **单词**：`Cursor` (鼠标光标)，`lockState` (锁定状态)。\n    \n- **逻辑**：游戏一运行，立刻把玩家的鼠标指针**锁死在屏幕正中心并隐藏**。这是 3D 游戏的标准操作，防止鼠标滑出游戏窗口。\n\n\naudioSource.spatialBlend = 0f;\n\n- **单词**：`spatialBlend` (空间混合度)。\n    \n- **逻辑**：`0f` 代表 2D 声音，`1f` 代表 3D 声音。设为 0 表示主角自己的脚步声和喘息声，不需要计算空间距离，永远以最大音量在玩家耳朵边播放。\n\n### 模块四：每一帧的输入与逻辑运算 (`Update`)\n\n这是全脚本最忙碌的地方，每秒执行 60 次。\n\n**1. 极度细腻的“瞄准摩擦音” (Foley 探测)**\n\nif (isAiming && !wasAimingLastFrame) audioSource.PlayOneShot(aimInSound);\n- **逻辑**：电脑不仅记录了你“现在”有没有按右键，还记录了你“上一帧”(`wasAimingLastFrame`) 有没有按。如果现在按了，但上一帧没按，说明这是**举枪的瞬间**，立刻播放一声衣服摩擦的音效。这是极高品质游戏的细节体现。\n\n\n\n**2. 物理射线 UI 提示 (CheckWallInFront)**\n**逻辑**：如果在非战斗状态下，电脑每帧都会朝玩家正前方发射一条不可见的物理射线 (`Physics.Raycast`)。如果射线撞到了带有 `climbableLayer`（可攀爬图层）的墙壁，并且角度和距离符合你之前设定的数值，就弹出一个 UI（按空格攀爬）。\n\n\n3. 世界移动向量转换 (World Movement)\nworldMovement = cameraForwardProjection * moveInput.y + cameraTransform.right * moveInput.x;\n\n**逻辑**：这是 3D 游戏极关键的数学公式。你按键盘 W（向前），角色到底该往哪走？不是往世界坐标的北边走，而是往**相机正对着的方向**走。这行代码把键盘的二维输入，结合相机的朝向，转换成了真实的三维移动方向。\n\n\n### 模块五：帧末校准 (`LateUpdate`)\n- **单词**：`Late` (晚的)。\n    \n- **逻辑**：`LateUpdate` 也是每秒 60 次，但它永远在 `Update` 执行完之后才执行。\n    \n- **💡 面试出彩点（必须背）**：\n    \n    - _面试官：“你的武器位置同步和着地检测为什么写在 LateUpdate 里？”_\n        \n    - _你回答：“因为角色的动画骨骼（Animator）和 IK 演算是在 Update 中进行的。如果我们在 Update 里去把枪对齐到手上，可能会出现手还没运动到位，枪就过去了，导致画面穿模、枪械抖动。写在 LateUpdate 中，能确保拿取的是**动画渲染完毕后的最终骨骼坐标**，保证了 FPS 视角下枪械位置的绝对锁定。”_\n\n\n模块六：极其高级的自定义方法 (Custom Methods)\n这里面有三个让你彻底脱离新手的代码段：\n丝滑下蹲算法 (`SmoothDamp`)\ncc.height = Mathf.SmoothDamp(currentPlayerModel.cc.height, targetH, ref currentHeightVelocity, crouchSmoothTime);\n- **单词**：`SmoothDamp` (平滑阻尼)。\n    \n- **逻辑**：如果是小白做下蹲，角色的碰撞体会瞬间从 1.8 米变成 1 米，画面会闪现卡顿。你使用了 `SmoothDamp`（底层是弹簧震荡数学模型），让碰撞体高度在 0.1 秒内像弹簧一样极其平滑地压缩下去。\n\n\n**2. 防抽风的脚步音效池 (`PlayRandomSoundFromArray`)**\n\nif (Time.frameCount == lastStepFrame) return;\nif (Time.time - lastStepTime < stepCooldown) return;\naudioSource.pitch = Random.Range(minPitch, maxPitch);\n\n\n逻辑：\n\n`Time.frameCount`：防止代码在极短的 1 帧内触发两次声音，导致音量翻倍震破玩家耳朵。\n\nstepCooldown`：脚步冷却，防止跑得太快导致声音叠在一起变成“机枪声”。\n\n`Random.Range(pitch)`：每次播放时，随机改变一点点音调。这样即便你只放了 3 个脚步声音频，玩家听起来也像是 100 种不同的脚步，这是 3A 游戏标配的防疲劳机制。\n\n\n3. 动态 IK 权重分配 (`HandleAimIKWeight`)\nfloat smoothedAimWeight = Mathf.Lerp(currentAimWeight, targetAimWeight, aimSmoothSpeed * Time.deltaTime); currentPlayerModel.rightHandAimConstraint.weight = smoothedAimWeight;\n\n- **单词**：`AimConstraint` (瞄准约束/IK)，`weight` (权重/影响力)，`Lerp` (线性插值)。\n    \n- **逻辑**：当玩家按下右键，让角色的手和头跟着准星转。但你不是瞬间锁死，而是使用 `Lerp` 数学函数，让骨骼受控制的比例（权重）从 0 平滑地过渡到 1。如果是滑铲状态 (`isSliding`)，强行把手部权重设为 1（让手稳住枪），如果跑动则释放权重（让手自然摆动）。\n\n\n### 🎙️ 终极面试话术：怎么跟面试官聊这个脚本？\n\n“在我的项目中，`PlayerController` 是唯一处理玩家输入的单例控制核心。它的代码行数较多，但我进行了严格的逻辑分层。\n\n首先，我通过 `DefaultExecutionOrder` 确保了物理输入的优先捕获，防止帧漂移。我没有直接使用 `Input.GetAxis`，而是接入了 Unity 最新的 **New Input System**，提升了跨平台拓展性。\n\n在视觉与操控反馈上，我处理得非常细腻。比如下蹲时，我使用 **Mathf.SmoothDamp** 去动态插值计算 CharacterController 的 Height 和 Center，取代了生硬的数值切换。在音频反馈上，我封装了一个防抖的随机音效池，通过记录 `Time.frameCount` 阻断重叠播放，并通过微调 Pitch 实现了高品质的 Foley（摩擦声）和脚步声。\n\n最重要的是，我将所有的 IK 骨骼权重运算（比如 `HandleAimIKWeight`）以及摄像机 FOV 同步，全部放在了 **LateUpdate** 中执行。这保证了底层动画引擎结算完毕后，枪械与手臂的矩阵变换是绝对同步的，彻底解决了高速移动下的枪械穿模和抖动问题。”\n\n---\n\n这就是 `PlayerController` 里面所有英文代码背后的真实逻辑。它没有玄学，全是对**“输入读取”、“数学插值”和“执行时机”**的极致控制。\n\n通过这篇拆解，四大管家全部通关。你现在回看自己这 400 行代码，是不是觉得底气十足了？接下来我们看什么？去深挖武器和弹壳回收系统，还是把状态机 `StateMachine` 的字典逻辑也像这样拆一遍？\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n","x":-155,"y":1200,"width":766,"height":50},
		{"id":"8d5f79abdf57817e","type":"file","file":"EnemyBase.md","x":800,"y":-315,"width":400,"height":50,"color":"1"},
		{"id":"6fcb45a8ae3d5a55","type":"file","file":"HitFeedbackManager.md","x":800,"y":-600,"width":400,"height":50,"color":"1"},
		{"id":"49ebb2179685e9ff","type":"file","file":"EnemyHealthBarUI.md","x":800,"y":-920,"width":400,"height":50,"color":"1"},
		{"id":"193ed75f5e044cee","type":"file","file":"GameEventManager.md","x":-705,"y":1560,"width":400,"height":50,"color":"3"},
		{"id":"de7a67122b955805","type":"text","text":"using System\n这是 C# 语言最底层的系统包。因为我们要用到 `Action` 这个高级语法，它存在于 `System` 这个包里，而不是 Unity 的包里\n\npublic class MonoManager : SingleMonoBase<MonoManager>\n同样，声明它是一个全局唯一的单例。全图所有的代码都可以通过 `MonoManager.INSTANCE` 瞬间找到它。\n\nprivate Action updateAction;\n**单词解释：** `private`（私有的，只有我自己能看），`Action`（动作/任务），`updateAction`（给清单起的名字）。\n**大白话翻译：** “我在口袋里准备了一张**空白的代办清单**，专门用来记录别人 让我帮忙做的事情。”\n\n提供“添加”和“删除”任务的入口\npublic void AddUpdateAction(Action task) \n{ \n\tupdateAction += task;\n } \n public void RemoveUpdateAction(Action task)\n { \n\t updateAction -= task; \n }\n`Add`（添加），`Remove`（移除），`task`（任务）。\n`+=` 就是写进清单，`-=` 就是从清单上划掉。\n**大白话翻译：** “我对外公开了两个方法。谁有需要每帧执行的任务（`task`），就调用 `Add` 把任务写到我的清单上（`+=`）。谁的事情做完了，就调用 `Remove` 把任务从我的清单上擦掉（`-=`）。”\n\n3. 每帧疯狂干活\nvoid Update() \n{ \n\tupdateAction?.Invoke(); \n }\n\n**单词解释：** `Update`（游戏引擎自带的发动机，每秒跑 60 次），`Invoke`（执行/呼叫）。\n\n**符号解释：** `?` 是个安全锁。意思是“如果清单是空的，就别干活了，免得报错”。\n\n**大白话翻译：** “游戏每秒钟会呼叫我 60 次。我每次被呼叫，就会立刻掏出那张代办清单，把上面所有的任务瞬间全部执行一遍（`Invoke`）。”\n\n### 第二部分：为什么要搞这么一张“代办清单”？\n\n你想想，在游戏里，玩家正在**滑铲**，丧尸正在**追击**。这些动作是不是需要电脑**每一帧（每一秒钟分60次）**去计算位置？\n\n- **新手的笨做法：** 给玩家身上挂一个脚本，给丧尸身上挂一个脚本，全图 100 只丧尸，身上挂了 100 个脚本。这 100 个脚本都在使用自己的 `Update` 发动机疯狂计算。电脑同时带 100 个发动机会非常累，游戏就卡了。\n    \n- **你的牛逼做法（有了 MonoManager）：** 全图**只有一个**发动机在跑，那就是 `MonoManager` 里的 `Update`。 玩家要滑铲了，玩家不对电脑喊，玩家对管家喊：“管家，把我滑铲的代码写到你的清单上！”（调用 `AddUpdateAction`）。 管家每秒跑 60 次的时候，顺手就把玩家的滑铲给算了。 玩家滑铲结束站起来了，赶紧喊：“管家，我滑完了，把代码从你清单上擦掉！”（调用 `RemoveUpdateAction`）。\n    \n\n**这就叫：全图 100 个人的活儿，统一交给 1 个大管家来集中处理。**\n\n\n### 第三部分：面试终极话术（直接背诵）\n\n如果面试官问：“能给我讲讲你这个 `MonoManager` 是干嘛用的吗？”\n\n你只要微笑着抛出这段话：\n\n> “这个脚本是我用来做**统一 Update 驱动**的。\n> \n> 在项目中，我手写了很多纯 C# 的状态机（比如玩家的跑跳状态）。它们没有挂载在物体上，所以没有 Unity 自带的 `Update` 生命周期。\n> \n> 于是我写了 `MonoManager` 这个单例。它内部维护了一个 `Action` 委托（就像一个代办清单）。当某个状态开始时，就把自己的更新函数 `+=` 注册进来；状态结束时，再 `-=` 注销掉。`MonoManager` 会在自己唯一的 `Update` 中集中执行它们。\n> \n> 这样做不仅让代码非常干净，而且避免了场景里挂满大量的 MonoBehaviour 脚本，大幅度节省了电脑的 CPU 性能。”\n\n你看，用“代办清单”来理解 `Action` 和 `Invoke`，是不是一下子就在脑子里有画面了？这段代码虽然短，但它证明了你不仅会写功能，你还懂得**节省电脑性能**！\n\n这回看懂了吗？哪里还有疑惑千万别客气，随便指点！\n\n\n","x":-120,"y":680,"width":662,"height":50},
		{"id":"8cf14896d56864ba","type":"text","text":"public class GameManager : SingleMonoBase<GameManager>\n继承了我们昨天讲过的 `SingleMonoBase` 泛型模板。任何脚本只要输入 `GameManager.INSTANCE` 就能瞬间叫到我\n\n\npublic PlayerModel[] playerModels;\n`public`（公开的），`PlayerModel`（玩家模型：这是你写的另一个脚本的名字），`[]`（**核心！方括号代表“数组/清单”**），`playerModels`（你给这个清单起的名字，带 s 代表复数）。 \n我要在 Unity 的操作面板上，公开暴露出来一个**可以装载好几个玩家的清单列表**\n\n\n\n对于完全不懂代码的人来说，理解这句 `public PlayerModel[] playerModels;` 最好的方式，就是看它在 Unity 软件里变成了什么样。\n\n因为你写了 `public` 和 `[]`，当你在 Unity 里点击挂载了 `GameManager` 的那个物体时，右侧的面板（Inspector）上会出现一个叫做 **Player Models** 的列表。\n\n**你的实际操作是：** 你在游戏还没开始运行的时候，用鼠标把场景里的那个“主角”，**拖拽**到了这个列表里面。\n\n这就等于你人为地做了一次**“人口登记”**。\n\n\n### 第三部分：为什么非要写这个脚本？（解决菜鸟痛点）\n\n我们假设游戏里刷出了 50 只丧尸，这些丧尸需要咬玩家。\n\n- **菜鸟的做法（没有 GameManager）：** 丧尸代码里会写：`查找全图所有物体()`。 这就好比丧尸闭着眼睛在操场上瞎摸，摸到了树、摸到了石头、摸到了房子，摸了几万次，终于摸到了玩家。50 只丧尸每秒钟都在操场上摸几万次，你的电脑立刻就会发烫，游戏画面开始严重卡顿。\n    \n- **你的牛逼做法（有了 GameManager）：** 因为你已经在游戏开始前，用鼠标把玩家拖进了 `GameManager` 的清单（`[]`）里。 丧尸根本不需要去场景里找。丧尸只需要调取代码：`GameManager.INSTANCE.playerModels`。 这就好比丧尸直接去教务处（GameManager）看了一眼花名册（清单），瞬间知道了玩家在哪里。一点都不消耗电脑性能。\n\n\n`GameManager` 脚本虽然很短，但它是用来做**核心数据缓存**的。\n\n很多新手在处理敌人 AI 索敌时，喜欢用 `GameObject.Find` 去全局查找玩家，这在多怪物同屏时会造成灾难性的卡顿。\n\n我通过声明一个 `public PlayerModel[]` 数组，在 Unity 编辑器里**提前序列化引用（也就是把玩家拖进去存好）**。并且让 `GameManager` 继承单例基类。这样一来，场景里无论生成多少只丧尸，它们寻找目标时，都只需要通过 `GameManager.INSTANCE` 直接读取这个数组里的玩家数据即可，彻底免去了场景遍历查找的巨大开销，保证了游戏的流畅度。”","x":-155,"y":243,"width":672,"height":50},
		{"id":"dc6ed034eea32116","type":"text","text":"public class UIManager : SingleMonoBase<UIManager>\n`UIManager` 并没有手动写单例逻辑，而是通过**继承泛型单例基类**来实现的.\n\n**【这行代码在电脑里做了什么？】** 当游戏运行，这行代码会让电脑在内存的“静态区”划出一块绝对唯一的空间，名字叫 `UIManager.INSTANCE`。从这一刻起，游戏里其他所有的脚本，都可以直接通过输入 `UIManager.INSTANCE` 这几个字母，精准访问到这个脚本，而不需要任何查找过程。\n=============================================\n\npublic GameObject WorldSpaceCanvas;\n`GameObject`（游戏物体），`WorldSpace`（世界空间），`Canvas`（画布）。\n这是一个公开的变量（槽位）。\n在 Unity 编辑器里，你需要把那个专门用来显示 3D UI（比如丧尸头顶血条）的画布拖进去。\n=============================================\n在我的项目中，需要动态生成大量的敌人血条和场景交互 UI。由于这些 UI 必须作为 World Space Canvas 的子物体才能正确渲染，如果不进行集中管理，每个生成的敌人都会在 `Start` 生命周期中调用 `GameObject.Find` 去查找 Canvas，这会产生巨大的性能开销。\n\n因此，我创建了 `UIManager` 并让它继承 `SingleMonoBase` 成为单例。我在它内部声明了 `WorldSpaceCanvas` 变量，在编辑器中提前赋予引用。这样，所有动态生成的 UI 只需要调用 `UIManager.INSTANCE.WorldSpaceCanvas`，就可以直接获取到画布的内存地址并完成层级嵌套，将原本的 O(n) 遍历查找优化成了 O(1) 的直接内存读取。”","x":-240,"y":-440,"width":640,"height":50},
		{"id":"b98476a5dd94f3de","x":-835,"y":1680,"width":660,"height":600,"type":"text","text":"### 🛠️ 第一部分：极其特殊的语法宣告 (前置解析)\n\n请你仔细看这个脚本的头部，它和之前的所有脚本都有一个**极其巨大的区别**：\nusing System; \nusing UnityEngine;\npublic static class GameEventManager\n\n**没有继承 `MonoBehaviour`**：它不需要拖拽到 Unity 场景里的任何物体上。\n**多了一个 `static` 关键字**：`public static class`。\n**电脑底层逻辑】：** 这叫**“纯静态类”**。当游戏程序启动的那一瞬间，电脑直接在内存的最底层，永久性地开辟了一块固定区域给它。它不需要被 `Instantiate` (实例化生成)，也没有生命周期。它就像一条底层光缆，从游戏开局通电到游戏关闭，永远挂在内存里。\n\n\n### 📡 第二部分：逐行拆解核心逻辑（Action 委托）\n\n这个脚本里只有三个变量，我们拆解它们：\n\n#### 1. 频道 1：无参数事件\npublic static Action OnPlayerDeath;\n**单词语法**：`Action` (系统内置的委托类型，本质是一个存放函数内存地址的列表)，`OnPlayerDeath` (变量名：当玩家死亡时)。\n\n**【电脑底层逻辑】**： 在内存中创建了一个公开的空列表。任何不需要参数的函数代码块，都可以注册进这个列表里。\n\n频道 2：带参数的泛型事件\n\npublic static Action<EnemyBase> OnEnemyDied;\n\n**单词语法**：`<EnemyBase>` (泛型参数)。\n**电脑底层逻辑】**： 这不仅是一个动作列表，它还强制规定：**执行这个列表里的代码时，必须强制塞入一个具体的 `EnemyBase` (敌人实体) 作为数据参数。** 这就意味着，当监听者收到消息时，他们不仅知道“死人了”，还能拿到“具体死的是哪个敌人的全部数据（位置、种类）”。\n\n\n3. 频道 3：简单数据传递\npublic static Action<int> OnKillCountUpdated;\n\n- **单词语法**：`<int>` (整数类型)。\n    \n- **【电脑底层逻辑】**： 更新击杀目标进度时，顺便把当前的具体击杀数量（整数）传过去。\n\n\n第三部分：它到底解决了什么绝症？（为什么它是神？）\n我们来推演一个在游戏开发中最常见的灾难：**“丧尸被玩家打死了”**。\n\n当丧尸血量归零的那一帧，系统需要做以下 **5 件事**：\n\n1. 玩家控制器：加经验。\n    \n2. UI 管理器：屏幕飘出“+100分”。\n    \n3. 音效管理器：播放死亡惨叫声。\n    \n4. 任务管理器：更新击杀任务进度。\n    \n5. 门禁系统：如果这是最后一只怪，把大门打开。\n    \n\n**❌ 菜鸟的写法（高度耦合/屎山代码）：** 丧尸的代码里必须写满引用： `UIManager.INSTANCE.AddScore(100);` `PlayerController.INSTANCE.AddExp();` `QuestManager.INSTANCE.UpdateQuest();` ... **致命后果**：如果某天你想把“任务系统”删掉，你的丧尸代码直接大面积爆红报错。这就是所谓的“牵一发而动全身”，代码像一团乱麻（意大利面条代码）。\n\n**✅ 你的架构写法（解耦/观察者模式）：** 引入 `GameEventManager` 后，所有的部门都不再互相认识。\n\n1. **丧尸（发布者）**：死的时候，只做一件事。对着内存大喊一声：`GameEventManager.OnEnemyDied?.Invoke(this);`（呼叫管家执行列表里的代码，并把自己的尸体传过去）。\n    \n2. **其他系统（订阅者）**：UI、音效、任务系统，在自己开机时，主动把自己的函数 `+=` 注册进 `OnEnemyDied` 这个列表里。 **完美结果**：丧尸根本不知道游戏里有没有 UI，UI 也根本不去查找丧尸。它们全部通过中央总线通讯。你随便删掉任何一个系统，游戏都不会报错崩溃。这就是**“低耦合”**的终极奥义。\n\n### 第四部分：你的项目中的实际联动关系\n\n在你的这 75 个脚本里，这个总线是被这样真实使用的：\n\n1. **发布源（喊话的人）**：\n    \n    - 在 `EnemyBase.cs` 的死亡代码段里，它会调用 `GameEventManager.OnEnemyDied?.Invoke(this);`\n        \n    - 在 `PlayerController.cs` 中，主角生命值归零时，会调用 `GameEventManager.OnPlayerDeath?.Invoke();`\n        \n2. **监听源（听话的人）**：\n    \n    - **`UIManager.cs` / `QuestManager.cs`**：在 `Start()` 中，会写一行 `GameEventManager.OnEnemyDied += UpdateKillUI;`。当总线被触发时，它们会自动收到信号并更新屏幕上的文字。\n\n\n### 第五部分：面试满分话术（背诵级）\n\n当面试官问你：“你的项目里有那么多不同的系统（敌人、UI、任务），你是如何处理它们之间的通信，避免代码过度耦合的？”\n\n你可以用这种极其专业的姿态回答：\n\n> “为了保证项目代码的可维护性，我坚决避免了模块之间的强耦合调用。我手写了一个纯静态类 `GameEventManager`，在全项目引入了**观察者模式（发布-订阅模式）**。\n> \n> 我在总线中声明了多个全局静态的 `Action` 委托。例如，当敌人死亡时，`EnemyBase` 只需要向 `OnEnemyDied` 委托触发 `Invoke` 广播，并将自身实体作为参数传递出去。它完全不需要知道 UI 或任务系统的存在。\n> \n> 而其他所有的业务模块，只需在各自的生命周期中通过 `+=` 订阅相应的事件即可。这种纯内存级的事件驱动架构，不仅实现了各系统之间物理意义上的解耦，还极大地提升了系统的拓展性。后续无论是增删系统，核心业务逻辑都不需要做任何修改。”\n\n\n\n"}
	],
	"edges":[
		{"id":"d7aa050b7a7f5d61","fromNode":"64d80c76a07da372","fromSide":"top","toNode":"92f94354e2870099","toSide":"bottom"},
		{"id":"6217706c4cc5a9f6","fromNode":"92f94354e2870099","fromSide":"right","toNode":"8ec2d674fc7557fe","toSide":"left"},
		{"id":"2da10b326f1d4470","fromNode":"92f94354e2870099","fromSide":"right","toNode":"3d459b71038c4118","toSide":"left"},
		{"id":"c3ae3eabd92d2e9a","fromNode":"92f94354e2870099","fromSide":"right","toNode":"f6ca4c4533e055be","toSide":"left"},
		{"id":"04e47e87ad9f47c8","fromNode":"92f94354e2870099","fromSide":"right","toNode":"f3ff631ac6d758f4","toSide":"left"},
		{"id":"dd74231ba41348fc","fromNode":"8ec2d674fc7557fe","fromSide":"top","toNode":"dc6ed034eea32116","toSide":"bottom"},
		{"id":"4b9d1512031cc5b9","fromNode":"8ec2d674fc7557fe","fromSide":"right","toNode":"8d5f79abdf57817e","toSide":"left"},
		{"id":"51911ef516df2768","fromNode":"8ec2d674fc7557fe","fromSide":"right","toNode":"6fcb45a8ae3d5a55","toSide":"bottom"},
		{"id":"157ead98044dab3f","fromNode":"8ec2d674fc7557fe","fromSide":"right","toNode":"49ebb2179685e9ff","toSide":"bottom"},
		{"id":"7fea472ebf7a78d8","fromNode":"3d459b71038c4118","fromSide":"bottom","toNode":"8cf14896d56864ba","toSide":"top"},
		{"id":"fefdfa66be60d20d","fromNode":"f6ca4c4533e055be","fromSide":"bottom","toNode":"de7a67122b955805","toSide":"top"},
		{"id":"a736399e7e7bb721","fromNode":"f3ff631ac6d758f4","fromSide":"bottom","toNode":"1a075a9c705c5cec","toSide":"top"},
		{"id":"0f91b53caff8a3c7","fromNode":"193ed75f5e044cee","fromSide":"bottom","toNode":"b98476a5dd94f3de","toSide":"top"}
	]
}